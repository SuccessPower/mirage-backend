# Mirage Backend

Production-oriented ASP.NET Core backend foundation for the Mirage relationship platform.

## Architecture

The backend is a modular monolith with strict project boundaries:

- `Mirage.Domain` — domain entities, invariants, and lifecycle states.
- `Mirage.Application` — use-case contracts and persistence abstractions.
- `Mirage.Infrastructure` — EF Core, PostgreSQL, Identity, migrations, and external adapters.
- `Mirage.Api` — versioned HTTP endpoints, JWT auth, rate limiting, health checks, and composition.

This shape keeps the launch deployment operationally simple while preserving extraction seams for
matching, messaging, counselling, notifications, and billing when scale or team ownership justifies it.

## Implemented foundation

- ASP.NET Core Identity registration/login with short-lived JWT access tokens.
- Rotating, SHA-256-hashed refresh tokens.
- PostgreSQL persistence through EF Core and Npgsql.
- Profile discovery and profile management.
- Organisation onboarding and user recommendations.
- Mutual-like matching.
- Open date requests, acceptance, and requester selection.
- Counsellor discovery, session booking, and audited mutual trust unlock.
- API-level anonymity projection for counsellor discovery.
- Pagination, database indexes, retries, connection-pool limits, rate limiting, Problem Details.
- Liveness/readiness probes, Docker, and Render Blueprint.

Vendor integrations from later BRD phases—social auth, media storage, SignalR messaging, WebRTC,
payments, OpenSearch, push notifications, and a durable event bus—are intentionally not faked.

## Local setup

```bash
cp .env.example .env
set -a
source .env
set +a
dotnet restore
./scripts/start-dev.sh
```

Development automatically applies pending migrations. Render runs `dotnet Mirage.Api.dll --migrate`
as a pre-deploy command after the image is built and before the new version starts. A failed migration
fails the deployment and leaves the previous service version running.

To apply migrations explicitly:

```bash
./scripts/apply-migrations.sh
```

After storing `DATABASE_URL` in the API's .NET User Secrets, EF commands also work directly:

```bash
cd src/Mirage.Api
dotnet ef database update
```

API documentation is available at `/swagger`. Health endpoints:

Opening the API root redirects to `/swagger/index.html`. Swagger is enabled by default in every
environment and can be disabled with `Swagger__Enabled=false`.

- `GET /health/live`
- `GET /health/ready`

Successful JSON responses use a consistent envelope:

```json
{
  "success": true,
  "message": "Profiles retrieved successfully.",
  "data": {},
  "meta": {
    "traceId": "0H...",
    "timestampUtc": "2026-06-22T11:19:08Z",
    "responseTimeMs": 42.371
  }
}
```

Errors use RFC 7807 `ProblemDetails`. Every response also includes `X-Response-Time-Ms` and
`Server-Timing`; the header value is authoritative because it includes response serialization.

## Logging

Serilog is the application-wide logging provider:

- Production emits compact structured JSON to stdout for Render log ingestion.
- Development emits readable console logs and EF Core SQL command diagnostics.
- Each request has one completion event with status code, elapsed milliseconds, correlation ID,
  trace ID, endpoint, host, protocol, client IP, user agent, and authenticated user ID.
- `X-Correlation-ID` is accepted from callers or generated by the API and returned in the response.
- Requests over two seconds and all 4xx responses are warnings; 5xx responses are errors.

Request/response bodies, authorization headers, JWTs, passwords, database credentials, and other
sensitive payloads are deliberately not logged.

## Database and secret handling

Set the Aiven connection URI as `DATABASE_URL`; do not place it in `appsettings.json`.
The URI is converted to an Npgsql connection string with TLS required, transient retries enabled,
and a conservative pool size because the current Aiven service has a low connection limit.

The database password supplied during initial setup was exposed in chat and should be rotated in
Aiven before production launch. Update only the Render secret after rotation.

## Render

Create the service from `render.yaml`, then provide:

- `DATABASE_URL`
- `Cors__AllowedOrigins__0` with the production Vercel origin

Render supplies `PORT`; the API binds to it automatically. Production startup migrations are disabled:
the Blueprint executes the app's dedicated `--migrate` mode once per deployment. PostgreSQL advisory
locking also prevents concurrent migration runners from modifying the schema simultaneously.

## Production follow-ups

- Add email verification, password reset, external Google/Apple providers, and refresh-token reuse detection.
- Add transactional outbox + broker before sending notifications from request handlers.
- Add KMS-backed envelope encryption for message bodies; TLS/database encryption is not end-to-end encryption.
- Add object storage with signed URLs and malware scanning for profile/verification media.
- Add OpenTelemetry traces/metrics and centralized structured logs.
- Add integration tests with ephemeral PostgreSQL in CI.
